document.querySelectorAll('[href="0"]').forEach(anchor => {
    anchor.addEventListener(click, funtion (e)) ({
        e:PreventDefault(),

        document:querySelector(this.GetAttribute('href')). scrollIntoview({
            behavior: 'smooth'
        }),
    });
});